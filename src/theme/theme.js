import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#84ffff",
      light: "#baffff",
      dark: "#4bcbcc",
    },
    secondary: {
      main: "#455a64",
      light: "#718792",
      dark: "#1c313a",
    },
  },
});

export default theme;
